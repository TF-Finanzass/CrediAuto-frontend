/**
 * Defines a standard structure for entities with a unique identifier in the infrastructure layer.
 */
export interface BaseEntity {
  /**
   * The unique identifier for the entity.
   */
  id: number;
}
